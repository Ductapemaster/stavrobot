# TODO items

Work on these one at a time. Mark them as done when the user confirms they're done:

[ ] Add a cron tool that the bot can call to add/edit cron entries. These cron entries will fire and send the bot a message from the "cron" system about whatever note it added for itself.
