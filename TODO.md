# TODO items

Work on these one at a time. Delete when the user confirms they're done:

- Add a second "tools" container, to which none of the LLMs have access to see/edit.
  This is for the user to install trusted tools in, that the LLMs can only call.
