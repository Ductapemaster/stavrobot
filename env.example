# Postgres credentials. These are passed to both the postgres and app containers.
# The defaults below match the out-of-the-box docker-compose setup.
# Copy this file to .env and uncomment/change values to override.

POSTGRES_USER=stavrobot
POSTGRES_PASSWORD=stavrobot
POSTGRES_DB=stavrobot
