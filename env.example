# Copy this file to .env and change values to override.

# Timezone for all containers. Affects log timestamps, cron scheduling, etc.
# Examples: America/New_York, Europe/London, Asia/Tokyo
# Full list: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=UTC

# Postgres credentials. Passed to both the postgres and app containers.
POSTGRES_USER=stavrobot
POSTGRES_PASSWORD=stavrobot
POSTGRES_DB=stavrobot

# Uncomment to enable optional services. Multiple profiles are comma-separated.
# "signal" starts the signal-bridge container.
# "ngrok" starts the ngrok container for a dynamic public HTTPS tunnel.
#COMPOSE_PROFILES=signal

# ngrok auth token. Required when using the "ngrok" profile.
# Get yours at https://dashboard.ngrok.com/get-started/your-authtoken
#NGROK_AUTHTOKEN=
